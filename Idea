The main idea is to do a simple classification of either wearing a mask or not by using a model which utilizes a CNN architecture. 240 set of images had been used for both training and test set respectively. Images has been gathered from various sources but mostly gathered through Kaggle & Google Image. 
